import { z } from "zod";import { requireApiProfile } from "@/lib/access-control";import { recordBusinessAudit } from "@/lib/audit";import { db } from "@/lib/db";import { consumeRateLimit,enforceSameOrigin,rateLimitResponse,readJsonWithLimit,requestParseError } from "@/lib/http-security";import { hasFreshFinancialMfa } from "@/lib/mfa";import { ensurePeriod } from "@/lib/restaurant-controls";import { refreshControlAlerts } from "@/lib/control-rules";
const m=z.number().min(0).max(1_000_000_000),sm=z.number().min(-1_000_000_000).max(1_000_000_000);const schema=z.object({outletId:z.string().uuid(),year:z.number().int().min(2020).max(2100),month:z.number().int().min(1).max(12),grossRevenue:m,netRevenue:m,totalOrders:z.number().int().min(0),discounts:m.default(0),refunds:m.default(0),foodCost:m.default(0),packagingCost:m.default(0),payrollCost:m.default(0),rentCost:m.default(0),utilitiesCost:m.default(0),aggregatorCommission:m.default(0),marketingCost:m.default(0),maintenanceCost:m.default(0),otherOperatingCost:m.default(0),grossProfit:sm,operatingProfit:sm,netProfit:sm,notes:z.string().max(1500).optional()});
export async function POST(req:Request){const same=enforceSameOrigin(req);if(same)return same;const a=await requireApiProfile("/api/monthly-financials");if("response" in a)return a.response;if(!(await hasFreshFinancialMfa()))return Response.json({error:"Fresh MFA required",code:"MFA_FRESH_REQUIRED"},{status:428});const lim=await consumeRateLimit("financial-write",20,60,"user");if(!lim.allowed)return rateLimitResponse(lim.retryAfter);try{const p=schema.safeParse(await readJsonWithLimit(req,16000));if(!p.success)return Response.json({error:"Invalid financial record",issues:p.error.flatten()},{status:400});const v=p.data,period=await ensurePeriod(v.year,v.month);if(period.status==="closed")return Response.json({error:"Reporting period is closed"},{status:409});const old=(await db()`SELECT * FROM outlet_monthly_financials WHERE outlet_id=${v.outletId} AND period_id=${period.id}`)[0] as any;const rows=await db()`INSERT INTO outlet_monthly_financials(outlet_id,period_id,gross_revenue,net_revenue,total_orders,discounts,refunds,food_cost,packaging_cost,payroll_cost,rent_cost,utilities_cost,aggregator_commission,marketing_cost,maintenance_cost,other_operating_cost,gross_profit,operating_profit,net_profit,notes,source) VALUES(${v.outletId},${period.id},${v.grossRevenue},${v.netRevenue},${v.totalOrders},${v.discounts},${v.refunds},${v.foodCost},${v.packagingCost},${v.payrollCost},${v.rentCost},${v.utilitiesCost},${v.aggregatorCommission},${v.marketingCost},${v.maintenanceCost},${v.otherOperatingCost},${v.grossProfit},${v.operatingProfit},${v.netProfit},${v.notes||null},'manual') ON CONFLICT(outlet_id,period_id) DO UPDATE SET gross_revenue=EXCLUDED.gross_revenue,net_revenue=EXCLUDED.net_revenue,total_orders=EXCLUDED.total_orders,discounts=EXCLUDED.discounts,refunds=EXCLUDED.refunds,food_cost=EXCLUDED.food_cost,packaging_cost=EXCLUDED.packaging_cost,payroll_cost=EXCLUDED.payroll_cost,rent_cost=EXCLUDED.rent_cost,utilities_cost=EXCLUDED.utilities_cost,aggregator_commission=EXCLUDED.aggregator_commission,marketing_cost=EXCLUDED.marketing_cost,maintenance_cost=EXCLUDED.maintenance_cost,other_operating_cost=EXCLUDED.other_operating_cost,gross_profit=EXCLUDED.gross_profit,operating_profit=EXCLUDED.operating_profit,net_profit=EXCLUDED.net_profit,notes=EXCLUDED.notes,source='manual',updated_at=NOW() RETURNING *`;const r=rows[0] as any;await recordBusinessAudit({actorUserId:a.profile.id,action:old?"update":"create",entityType:"outlet_monthly_financial",entityId:String(r.id),outletId:v.outletId,oldValues:old,newValues:r});await refreshControlAlerts();return Response.json({ok:true,row:r});}catch(e){const r=requestParseError(e);if(r)return r;throw e;}}
