import Image from "next/image";
import { getCurrentProfileForAudit } from "@/lib/authz";
export const dynamic="force-dynamic";
export default async function DevicePending(){const profile=await getCurrentProfileForAudit();return <main className="secure-gate"><section className="secure-gate__brand"><Image src="/grub-monkeys-logo.png" width={232} height={84} alt="Grub Monkeys" priority/><span>COMMAND CENTER / DEVICE CONTROL</span><h1>Access held.</h1><p>This browser is waiting for Owner approval. No outlet, financial or employee data is returned while the device is pending.</p></section><section className="auth-control-card"><span className="auth-kicker">PENDING / {profile.role.toUpperCase()}</span><h2>Device approval required.</h2><div className="pending-beacon"><i/><span><b>WAITING FOR OWNER</b><small>Approval can be completed from Security → Devices.</small></span></div><a className="command-button full" href="/overview">CHECK AGAIN <span>→</span></a></section></main>;}
