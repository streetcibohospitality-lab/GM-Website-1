# Design previews

These PNGs are browser renders of the implemented GM Command Center layout/CSS with representative demo markup. They are not AI-generated dashboard mockups.

The sandbox used local fallback typography because external package/font downloads were unavailable. The actual Next.js source uses Jaro and Lexend via `next/font`.
